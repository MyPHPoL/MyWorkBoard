import { FilterPriorityPipe } from './filter-priority.pipe';

describe('FilterPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
